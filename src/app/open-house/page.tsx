const OpenHouse = () => {
    return (
        <section className="w-full h-screen grid">
            <iframe
                title="mapBox"
                src="https://inkyphotos.pixieset.com/2026bijouhomesopenday/"
                className="w-full h-screen border-0"
                loading="lazy"
                referrerPolicy="no-referrer-when-downgrade"
            />
        </section>
    );
};

export default OpenHouse;